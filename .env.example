# Prebuilt image reference (same for both, only env vars differ)
BLUE_IMAGE=
GREEN_IMAGE=

# Which pool is active by default (blue or green)
ACTIVE_POOL=

# Each release ID will be echoed back in response headers
RELEASE_ID_BLUE=
RELEASE_ID_GREEN=

# Internal port app listens on (defined inside image)
PORT=
